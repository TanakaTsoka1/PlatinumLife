
@using PlatinumLife.Models
@model IEnumerable<Invoice>
@{

    ViewData["Title"] = "List of Invoices";
}

<div style="position: fixed; right: 25px; top:25px" class="text-class"  >
    <a  class="button-add" asp-controller="Invoice" asp-action="Create">
    <i class="bi-plus-circle"></i> Add New </a>
</div>

<div class="row">

    <div class="col-md-8 offset-md-2">
        <table>
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Submitter)</th>
                    <th>@Html.DisplayNameFor(model => model.Department)</th>
                    <th>@Html.DisplayNameFor(model => model.PaymentRecipientName)</th>
                    <th>@Html.DisplayNameFor(model => model.PaymentDescription)</th>
                    <th>Actions</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Submitter)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Department)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.PaymentRecipientName)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.PaymentDescription)
                        </td>
                        <td class="align-middle">
                            <a class="button-edit" asp-for="Invoice" asp-action="Edit" asp-route-id="@item.InvoiceId"><i class="bi bi-pencil-square"></i>Edit </a>
                        </td>
                        <td class="align-middle">
                            <a class="button-details" asp-for="Invoice" asp-action="Details" asp-route-id="@item.InvoiceId"><i class="bi bi-eye"></i>Details</a>
                        </td>
                        <td class="align-middle">
                            <a class="button-upload" asp-controller="FileUpload" asp-action="Index"><i class="bi bi-paperclip"></i>Upload POP</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>

